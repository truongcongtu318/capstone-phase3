# Copyright The OpenTelemetry Authors
# SPDX-License-Identifier: Apache-2.0
# rebuild-sync (retry after checkout main.go fix): touch to build alongside accounting/cart/checkout/product-catalog/product-reviews/recommendation under one CI tag

static_resources:
  listeners:
    - address:
        socket_address:
          address: "${ENVOY_ADDR}"
          port_value: ${ENVOY_PORT}
      filter_chains:
        - filters:
            - name: envoy.filters.network.http_connection_manager
              typed_config:
                "@type": type.googleapis.com/envoy.extensions.filters.network.http_connection_manager.v3.HttpConnectionManager
                codec_type: AUTO
                stat_prefix: ingress_http
                tracing:
                  spawn_upstream_span: true
                  provider:
                    name: envoy.tracers.opentelemetry
                    typed_config:
                      "@type": type.googleapis.com/envoy.config.trace.v3.OpenTelemetryConfig
                      grpc_service:
                        envoy_grpc:
                          cluster_name: opentelemetry_collector_grpc
                        timeout: 0.250s
                      service_name: ${OTEL_SERVICE_NAME}
                      resource_detectors:
                        - name: envoy.tracers.opentelemetry.resource_detectors.environment
                          typed_config:
                            "@type": type.googleapis.com/envoy.extensions.tracers.opentelemetry.resource_detectors.v3.EnvironmentResourceDetectorConfig
                route_config:
                  name: local_route
                  virtual_hosts:
                    - name: frontend
                      domains:
                        - "*"
                      routes:
                        # Mandate #1 (network-exposure): /loadgen, /jaeger, /grafana, /feature
                        # (flagd-ui) removed from the public route - ops UIs, private-only now
                        # (SSM bastion + kubectl port-forward, see docs/runbooks/private-access-to-ops-uis.md).
                        # /flagservice/ is NOT a UI - it's the flag-read path services depend on
                        # at runtime; do not remove it, that's a disqualifying flagd violation.
                        - match: { prefix: "/otlp-http/" }
                          route: { cluster: opentelemetry_collector_http, prefix_rewrite: "/" }
                        - match: { prefix: "/images/" }
                          route: { cluster: image-provider, prefix_rewrite: "/" }
                        - match: { prefix: "/flagservice/" }
                          route: { cluster: flagservice, prefix_rewrite: "/", timeout: 0s }
                        # PM-154 (load-shedding): /api/checkout protected — đặt TRƯỚC
                        # catch-all "/" để match trước. Không có typed_per_filter_config
                        # local_ratelimit → dùng global bucket 10 000 token/s → không bao
                        # giờ bị shed. Checkout = luồng tiền, ưu tiên tuyệt đối.
                        - match: { prefix: "/api/checkout" }
                          name: checkout_protected
                          route: { cluster: frontend }
                        # PM-154 (load-shedding): /api/cart protected — cart write ops
                        # (add/update/delete item) cùng nhóm ưu tiên với checkout;
                        # không bị shed để giữ session mua hàng trong khi browse bị hạn.
                        - match: { prefix: "/api/cart" }
                          name: cart_protected
                          route: { cluster: frontend }
                        # PM-154 (load-shedding): /api/products/<id> protected — product
                        # detail là bước đầu của checkout journey:
                        #   locustfile.py add_to_cart():
                        #     GET /api/products/<id>  ← bước này
                        #     POST /api/cart           ← cart_protected
                        #     POST /api/checkout       ← checkout_protected
                        # Prefix "/api/products/" (có trailing slash) CHỈ match detail
                        # pages, KHÔNG match GET /api/products (list) — list là browse.
                        # Nếu detail bị shed → add_to_cart fail → checkout funnel vỡ.
                        - match: { prefix: "/api/products/" }
                          name: product_detail_protected
                          route: { cluster: frontend }
                        # Shopping Copilot (AIO02) — public storefront integration.
                        # /api/copilot/<x> -> shopping-copilot /api/<x> (chat, confirm, cart).
                        # PUBLIC + unauthenticated LLM endpoint: per-route local rate limit
                        # bounds how many requests reach Bedrock, capping worst-case token
                        # cost from anonymous/bot abuse (app-level user_id limit is spoofable).
                        # timeout 20s covers the app's 15s hard fallback.
                        - match: { prefix: "/api/copilot/" }
                          name: copilot
                          route:
                            cluster: shopping-copilot
                            prefix_rewrite: "/api/"
                            timeout: 20s
                          typed_per_filter_config:
                            envoy.filters.http.local_ratelimit:
                              "@type": type.googleapis.com/envoy.extensions.filters.http.local_ratelimit.v3.LocalRateLimit
                              stat_prefix: copilot_rate_limiter
                              # ~2 req/s sustained per proxy (burst 120). With >=2 proxies the
                              # aggregate cap bounds Bedrock spend. Tune via the values below.
                              token_bucket:
                                max_tokens: 120
                                tokens_per_fill: 120
                                fill_interval: 60s
                              filter_enabled:
                                default_value:
                                  numerator: 100
                                  denominator: HUNDRED
                              filter_enforced:
                                default_value:
                                  numerator: 100
                                  denominator: HUNDRED
                              response_headers_to_add:
                                - header:
                                    key: x-techx-rate-limit
                                    value: copilot
                                  keep_empty_value: true
                        # PM-154 (load-shedding): browse/homepage catch-all — SHADOW.
                        # Requests above the calibrated per-proxy token bucket increment
                        # counters but are not rejected. Promote enforcement gradually
                        # only after route matching and checkout SLO are verified.
                        #
                        # The bootstrap budget below is derived from PM-152. Recalculate
                        # it from measured new-ceiling evidence after the after-run.
                        - match: { prefix: "/" }  # catch-all — giữ cuối
                          name: browse_shedable
                          route: { cluster: frontend }
                          typed_per_filter_config:
                            envoy.filters.http.local_ratelimit:
                              "@type": type.googleapis.com/envoy.extensions.filters.http.local_ratelimit.v3.LocalRateLimit
                              stat_prefix: browse_rate_limiter
                              token_bucket:
                                # PM-152 highest passing throughput = 174.75 RPS.
                                # The canonical profile is ~70% browse. Reserve a
                                # 10% system margin before dividing by the minimum
                                # two Ready proxies:
                                # floor(((174.75 * 0.70) - (174.75 * 0.10)) / 2)
                                # = 52 RPS/proxy. During shadow we allow a small
                                # burst (100 tokens) while measuring a sustained
                                # fill rate of 50 RPS/proxy. Recalibrate both from
                                # the measured new ceiling after the after-run.
                                max_tokens: ${BROWSE_RATE_LIMIT_MAX_TOKENS}
                                tokens_per_fill: ${BROWSE_RATE_LIMIT_TOKENS_PER_FILL}
                                fill_interval: ${BROWSE_RATE_LIMIT_FILL_INTERVAL}
                              filter_enabled:
                                runtime_key: browse_rate_limit_enabled
                                default_value:
                                  numerator: ${BROWSE_RATE_LIMIT_ENABLED_PERCENT}
                                  denominator: HUNDRED
                              filter_enforced:
                                runtime_key: browse_rate_limit_enforced
                                default_value:
                                  numerator: ${BROWSE_RATE_LIMIT_ENFORCED_PERCENT}
                                  denominator: HUNDRED
                              response_headers_to_add:
                                # Distinguishes intentional shedding from backend failures.
                                - header:
                                    key: x-techx-load-shed
                                    value: browse
                                  keep_empty_value: true
                http_filters:
                  - name: envoy.filters.http.fault
                    typed_config:
                      "@type": type.googleapis.com/envoy.extensions.filters.http.fault.v3.HTTPFault
                      max_active_faults: 100
                      delay:
                        header_delay: {}
                        percentage:
                          numerator: ${LOCAL_RATE_LIMIT_ENABLED_PERCENT}
                  # Mandate-19 (load-shedding): Local rate limit filter — in-process
                  # token bucket chạy trực tiếp trong Envoy, không cần Redis external.
                  # Global bucket đặt cực cao (10 000/s) vì việc giới hạn thực tế
                  # được cấu hình per-route trên browse_shedable route phía trên.
                  # checkout_protected route KHÔNG có per-route override → dùng
                  # global bucket này → không bao giờ bị shed (10k >> thực tế).
                  - name: envoy.filters.http.local_ratelimit
                    typed_config:
                      "@type": type.googleapis.com/envoy.extensions.filters.http.local_ratelimit.v3.LocalRateLimit
                      stat_prefix: local_rate_limiter
                      # Global bucket: cực cao — chỉ làm fallback, per-route override
                      # trên browse_shedable mới là bucket thực tế cần quan tâm.
                      token_bucket:
                        max_tokens: 10000
                        tokens_per_fill: 10000
                        fill_interval: 1s
                      filter_enabled:
                        runtime_key: local_rate_limit_enabled
                        default_value:
                          numerator: 100
                          denominator: HUNDRED
                      filter_enforced:
                        runtime_key: local_rate_limit_enforced
                        default_value:
                          numerator: ${LOCAL_RATE_LIMIT_ENFORCED_PERCENT}
                          denominator: HUNDRED
                  - name: envoy.filters.http.router
                    typed_config:
                      "@type": type.googleapis.com/envoy.extensions.filters.http.router.v3.Router
                access_log:
                  - name: envoy.access_loggers.open_telemetry
                    typed_config:
                      "@type": "type.googleapis.com/envoy.extensions.access_loggers.open_telemetry.v3.OpenTelemetryAccessLogConfig"
                      common_config:
                        log_name: "otel_envoy_access_log"
                        grpc_service:
                          envoy_grpc:
                            cluster_name: opentelemetry_collector_grpc
                        transport_api_version: "V3"
                      body:
                        # yamllint disable-line rule:line-length
                        string_value: "[%START_TIME%] \"%REQ(:METHOD)% %REQ(X-ENVOY-ORIGINAL-PATH?:PATH)% %PROTOCOL%\" %RESPONSE_CODE% %RESPONSE_FLAGS% %RESPONSE_CODE_DETAILS% %CONNECTION_TERMINATION_DETAILS% \"%UPSTREAM_TRANSPORT_FAILURE_REASON%\" %BYTES_RECEIVED% %BYTES_SENT% %DURATION% %RESP(X-ENVOY-UPSTREAM-SERVICE-TIME)% \"%REQ(X-FORWARDED-FOR)%\" \"%REQ(USER-AGENT)%\" \"%REQ(X-REQUEST-ID)%\" \"%REQ(:AUTHORITY)%\" \"%UPSTREAM_HOST%\" %UPSTREAM_CLUSTER% %UPSTREAM_LOCAL_ADDRESS% %DOWNSTREAM_LOCAL_ADDRESS% %DOWNSTREAM_REMOTE_ADDRESS% %REQUESTED_SERVER_NAME% %ROUTE_NAME%\n"
                      resource_attributes:
                        values:
                          - key: "service.name"
                            value:
                              string_value: ${OTEL_SERVICE_NAME}
                      attributes:
                        values:
                          - key: "destination.address"
                            value:
                              string_value: "%UPSTREAM_REMOTE_ADDRESS_WITHOUT_PORT%"
                          - key: "event.name"
                            value:
                              string_value: "proxy.access"
                          - key: "server.address"
                            value:
                              string_value: "%DOWNSTREAM_LOCAL_ADDRESS%"
                          - key: "source.address"
                            value:
                              string_value: "%DOWNSTREAM_REMOTE_ADDRESS_WITHOUT_PORT%"
                          - key: "upstream.cluster"
                            value:
                              string_value: "%UPSTREAM_CLUSTER%"
                          - key: "upstream.host"
                            value:
                              string_value: "%UPSTREAM_HOST%"
                          - key: "user_agent.original"
                            value:
                              string_value: "%REQ(USER-AGENT)%"
                          - key: "url.full"
                            value:
                              string_value: "%REQ(:SCHEME)%://%REQ(:AUTHORITY)%%REQ(:PATH)%"
                          - key: "url.path"
                            value:
                              string_value: "%REQ(:PATH)%"
                          - key: "url.query"
                            value:
                              string_value: "%REQ(:QUERY)%"
                          - key: "url.template"
                            value:
                              string_value: "%ROUTE_NAME%"
  clusters:
    - name: opentelemetry_collector_grpc
      type: STRICT_DNS
      lb_policy: ROUND_ROBIN
      typed_extension_protocol_options:
        envoy.extensions.upstreams.http.v3.HttpProtocolOptions:
          "@type": type.googleapis.com/envoy.extensions.upstreams.http.v3.HttpProtocolOptions
          explicit_http_config:
            http2_protocol_options: {}
      load_assignment:
        cluster_name: opentelemetry_collector_grpc
        endpoints:
          - lb_endpoints:
              - endpoint:
                  address:
                    socket_address:
                      address: ${OTEL_COLLECTOR_HOST}
                      port_value: ${OTEL_COLLECTOR_PORT_GRPC}
    - name: opentelemetry_collector_http
      type: STRICT_DNS
      lb_policy: ROUND_ROBIN
      load_assignment:
        cluster_name: opentelemetry_collector_http
        endpoints:
          - lb_endpoints:
              - endpoint:
                  address:
                    socket_address:
                      address: ${OTEL_COLLECTOR_HOST}
                      port_value: ${OTEL_COLLECTOR_PORT_HTTP}
    - name: frontend
      type: STRICT_DNS
      lb_policy: ROUND_ROBIN
      # circuit_breakers (max_requests: 4096) được tách sang PM-153 PR riêng
      # để giữ PM-154 focus vào route classification + rate-limit only.
      load_assignment:
        cluster_name: frontend
        endpoints:
          - lb_endpoints:
              - endpoint:
                  address:
                    socket_address:
                      address: ${FRONTEND_HOST}
                      port_value: ${FRONTEND_PORT}
    - name: shopping-copilot
      type: STRICT_DNS
      lb_policy: ROUND_ROBIN
      load_assignment:
        cluster_name: shopping-copilot
        endpoints:
          - lb_endpoints:
              - endpoint:
                  address:
                    socket_address:
                      address: shopping-copilot
                      port_value: 8001
    - name: image-provider
      type: STRICT_DNS
      lb_policy: ROUND_ROBIN
      load_assignment:
        cluster_name: image-provider
        endpoints:
          - lb_endpoints:
              - endpoint:
                  address:
                    socket_address:
                      address: ${IMAGE_PROVIDER_HOST}
                      port_value: ${IMAGE_PROVIDER_PORT}
    - name: flagservice
      type: STRICT_DNS
      lb_policy: ROUND_ROBIN
      load_assignment:
        cluster_name: flagservice
        endpoints:
          - lb_endpoints:
              - endpoint:
                  address:
                    socket_address:
                      address: ${FLAGD_HOST}
                      port_value: ${FLAGD_PORT}
    - name: flagd-ui
      type: STRICT_DNS
      lb_policy: ROUND_ROBIN
      load_assignment:
        cluster_name: flagd-ui
        endpoints:
          - lb_endpoints:
              - endpoint:
                  address:
                    socket_address:
                      address: ${FLAGD_UI_HOST}
                      port_value: ${FLAGD_UI_PORT}
    - name: loadgen
      type: STRICT_DNS
      lb_policy: ROUND_ROBIN
      load_assignment:
        cluster_name: loadgen
        endpoints:
          - lb_endpoints:
              - endpoint:
                  address:
                    socket_address:
                      address: ${LOCUST_WEB_HOST}
                      port_value: ${LOCUST_WEB_PORT}
    - name: grafana
      type: STRICT_DNS
      lb_policy: ROUND_ROBIN
      load_assignment:
        cluster_name: grafana
        endpoints:
          - lb_endpoints:
              - endpoint:
                  address:
                    socket_address:
                      address: ${GRAFANA_HOST}
                      port_value: ${GRAFANA_PORT}
    - name: jaeger
      type: STRICT_DNS
      lb_policy: ROUND_ROBIN
      load_assignment:
        cluster_name: jaeger
        endpoints:
          - lb_endpoints:
              - endpoint:
                  address:
                    socket_address:
                      address: ${JAEGER_HOST}
                      port_value: ${JAEGER_UI_PORT}
admin:
  address:
    socket_address:
      address: 0.0.0.0
      port_value: ${ENVOY_ADMIN_PORT}
layered_runtime:
  layers:
    - name: static_layer_0
      static_layer:
        envoy:
          resource_limits:
            listener:
              example_listener_name:
                connection_limit: 10000
